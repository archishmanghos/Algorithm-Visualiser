import React from "react";

export default function AlgorithmCard(props){
    return (
        <div className="card algorithm--card--container">
            <h4>{props.name}</h4>
        </div>
    )
}